# Start from the official MySQL 8.0 image
FROM mysql:8.0

# Set environment variables (override in Kubernetes if needed)
ENV MYSQL_ROOT_PASSWORD=password \
    MYSQL_DATABASE=employees

# Copy initialization SQL file into container
COPY ./mysql.sql /docker-entrypoint-initdb.d/

# No need to override CMD — official image will auto-run init scripts
